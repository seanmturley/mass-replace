[package]
name = "mass_replace"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "mass-replace"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2.2.2"
predicates = "3.1.4"
